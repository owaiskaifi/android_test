version: 0.2

phases:
  build:
    commands:
     -  chmod -R 0777 /opt/android-sdk
     - yes | ~/Android/Sdk/tools/bin/sdkmanager --licenses
      - chmod +x gradlew
      - ./gradlew assembleDebug
artifacts:
  files:
    - app/build/outputs/apk/app-debug.apk
